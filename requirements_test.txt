# Test dependencies for Google Home Exposure Manager
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# Home Assistant dependencies for testing
homeassistant>=2024.1.0
voluptuous>=0.13.0

# YAML handling (same as production)
ruamel.yaml>=0.18.0
